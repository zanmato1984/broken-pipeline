@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@BROKEN_PIPELINE_SCHEDULE_ENABLED@)
  include("${CMAKE_CURRENT_LIST_DIR}/arrow_targets.cmake")
  find_dependency(folly CONFIG)
  find_dependency(glog CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/broken_pipelineTargets.cmake")
