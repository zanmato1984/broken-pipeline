#pragma once

/**
 * @file task_status.h
 *
 * @brief Compatibility header.
 *
 * `TaskStatus` now lives in `openpipeline/task/task.h`. This header remains for codebases
 * that still include `<openpipeline/task/task_status.h>`.
 */
#include <openpipeline/task/task.h>
